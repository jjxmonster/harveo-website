import styled from 'styled-components';

export const StyledSectionsContainer = styled.div`
   width: 100%;
   height: 85%;
   position: relative;
   overflow-x: hidden;
   > .pagination {
      position: absolute;
      bottom: 5%;
      left: 0;
      right: 0;
      margin: auto;
      width: 300px;
   }
`;
